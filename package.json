{
  "name": "developer-docs-progress-tracker",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "postinstall": "wxt prepare",
    "dev": "wxt",
    "build": "npm run build:chrome && npm run build:firefox",
    "build:chrome": "wxt build -b chrome --mv3",
    "build:firefox": "wxt build -b firefox --mv3",
    "zip": "npm run zip:chrome && npm run zip:firefox",
    "zip:chrome": "wxt zip -b chrome --mv3",
    "zip:firefox": "wxt zip -b firefox --mv3",
    "test": "vitest run",
    "test:e2e": "npm run build && playwright test tests/e2e",
    "test:e2e:common": "npm run build && playwright test tests/e2e/common.spec.ts",
    "test:e2e:sites": "npm run build && playwright test tests/e2e/sites.spec.ts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@wxt-dev/module-react": "^1.1.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "wxt": "^0.20.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/chrome": "^0.0.326",
    "@types/node": "^24.0.0",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
